# Waybar launcher: loads config.jsonc (dual-monitor, HDMI+eDP) when HDMI-A-1
# is connected AND active, config-laptop.jsonc (laptop-only, eDP 1-10) otherwise.
# Reloads automatically on output hotplug/profile change via sway output events.

exec uwsm app -- "$HOME/dotfiles/waybar/launch.sh"
