# # ~/.config/tmux/themes/sunset-drive.conf
#
# # ── Status bar genel ───────────────────────────────────────────────────────────
# set-option -g status          "on"
# set -g status-interval        2
# set-option -g status-style    fg='#ededfe',bg='#252535',default
# set-option -g status-left-style  none
# set-option -g status-right-style none
#
# # ── Mode ───────────────────────────────────────────────────────────────────────
# set-option -g mode-style      fg='#0f0f1a',bg='#ff57fd'
#
# # ── Pane sınırları ─────────────────────────────────────────────────────────────
# set-option -g pane-border-style        fg='#3e3e4b'
# set-option -g pane-active-border-style fg='#00ffed'
#
# # ── Sol durum çubuğu ───────────────────────────────────────────────────────────
# set -g status-left-length 60
# set -g status-left '#[fg=#0f0f1a,bg=#00f992,bold] #S #[fg=#ededfe,bg=#3e3e4b,nobold] #(whoami) #[fg=#3e3e4b,bg=#252535]'
#
# # ── Sağ durum çubuğu ───────────────────────────────────────────────────────────
# set -g status-right-length 120
# set -g status-right '#[fg=#252535,bg=#252535]#[fg=#ededfe,bg=#252535] %Y-%m-%d #[fg=#0f0f1a,bg=#00a4ff,bold] %H:%M #[fg=#0f0f1a,bg=#00ffed,bold] #h '
#
# # ── Pencere sekmeleri ──────────────────────────────────────────────────────────
# set -g window-status-separator ''
# set -g window-status-format         "#[fg=#8888a0,bg=#252535]  #I: #W  "
# set -g window-status-current-format "#[fg=#0f0f1a,bg=#ff57fd,bold]  #I: #W  "
# set-window-option -g window-status-activity-style fg='#ff0063',bg='#252535'




# ─── Renk Temel Değişkenleri ─────────────────────────────────
# bg:  #0f0f1a  fg:  #ededfe
# red: #ff0063  grn: #00f992  yel: #ffe900
# blu: #00a4ff  mag: #ff57fd  cyn: #00ffed
# dim: #3e3e4b  br-red: #ff948b  br-cyn: #38ffff

# ─── Genel ───────────────────────────────────────────────────
set -g default-terminal "tmux-256color"
set -ag terminal-overrides ",*:Tc"   # True color
set -g status-interval 1

# ─── Status Bar ──────────────────────────────────────────────
set -g status on
set -g status-position bottom
set -g status-justify left

# Status bar arkaplan / ön plan
set -g status-style "bg=#0f0f1a,fg=#ededfe"

# ─── Sol (session adı) ───────────────────────────────────────
set -g status-left         "#[bg=#00ffed,fg=#0f0f1a,bold] #S #[bg=#0f0f1a,fg=#00ffed,nobold]"
set -g status-left-length  30


# ─── Sağ (saat + tarih) ──────────────────────────────────────
set -g status-right-length 50
set -g status-right "#[fg=#3e3e4b]#[bg=#3e3e4b,fg=#00ffed] %H:%M #[bg=#3e3e4b,fg=#ededfe]│#[fg=#ededfe] %d/%m/%Y #[bg=#3e3e4b,fg=#0f0f1a]"

# ─── Window Listesi ──────────────────────────────────────────
set -g window-status-format         "#[bg=#0f0f1a,fg=#3e3e4b]▎#[fg=#5a5a6e] #I #[fg=#8888a0]#{=10:window_name}  "
set -g window-status-style          "bg=#0f0f1a"
set -g window-status-separator      ""


# Aktif pencere
set -g window-status-current-format "#[bg=#1a1a2e,fg=#00ffed]▎#[fg=#00ffed] #I #[fg=#ededfe]#{=10:window_name}  "
set -g window-status-current-style  "bg=#1a1a2e"

# Aktivite olan pencere
set -g window-status-activity-style "bg=#0f0f1a,fg=#ffe900"
set -g window-status-bell-style     "bg=#0f0f1a,fg=#ff0063,bold"

# ─── Pane Kenarlıkları ───────────────────────────────────────
set -g pane-border-style        "fg=#3e3e4b"
set -g pane-active-border-style "fg=#00a4ff"

# ─── Mesaj / Command Satırı ──────────────────────────────────
set -g message-style        "bg=#3e3e4b,fg=#00ffed,bold"
set -g message-command-style "bg=#3e3e4b,fg=#ff57fd,bold"

# ─── Copy Mode ───────────────────────────────────────────────
set -g mode-style "bg=#3e3e4b,fg=#ffe900"

# ─── Pane Numaraları ─────────────────────────────────────────
set -g display-panes-colour        "#3e3e4b"
set -g display-panes-active-colour "#00a4ff"

# ─── Diğer ───────────────────────────────────────────────────
set -g base-index 1
set -g pane-base-index 1
set -g renumber-windows on
